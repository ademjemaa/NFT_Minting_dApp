import { SmartContractResult } from 'types';
export declare function areScCallsSuccessful(scResults?: SmartContractResult[]): boolean;
export declare function isContract(receiver: string, data?: string): boolean;
export declare function getAddressFromDataField({ receiver, data }: {
    receiver: string;
    data: string;
}): string | undefined;
export default isContract;
