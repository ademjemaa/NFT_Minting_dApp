interface ReplyUrlType {
    callbackUrl: string;
    urlParams?: {
        [key: string]: string;
    };
}
export declare function buildReplyUrl({ callbackUrl, urlParams }: ReplyUrlType): string;
export default buildReplyUrl;
